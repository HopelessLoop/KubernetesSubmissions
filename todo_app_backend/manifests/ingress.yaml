apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-app-backend-ingress
spec:
  rules:
  - http:
      paths:
      - path: /todos
        pathType: Prefix
        backend:
          service:
            name: todo-app-backend-svc
            port:
              number: 18083